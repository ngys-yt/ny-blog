on:
  push:
    branches:
      - main

      - main
      - 'releases/**'

    # 具体的な実行内容を定義
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Lint Check
      - run-name: Lint Check
        run: yarn run lint

      - name: Type Check

      - run-name: Type Check
        run: yarn run tsc
# CItest
